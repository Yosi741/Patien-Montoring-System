Place optional blank certificate templates here:

- birth_template.pdf
- death_template.pdf

The current project uses the internal fallback PDF generator when no template is present. Full text/image overlay on an existing PDF template can be enabled later by adding a PDF library such as Apache PDFBox.
