import app.core.AppShell;

/**
 * Starts ClinicPulse through the JavaFX application shell.
 * This is the official desktop application entry point.
 */
public class Main {
    /**
     * Starts the ClinicPulse JavaFX desktop application.
     */
    public static void main(String[] args) {
        AppShell.launchApp(args);
    }
}
